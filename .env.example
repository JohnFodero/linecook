# Roboflow API Configuration
# Get your API key from https://roboflow.com/
ROBOFLOW_API_KEY=your_roboflow_api_key_here

# Model Configuration
# The Roboflow model ID for shipping label detection
MODEL_ID=shipping-label-k3hzg/4

# Inference Configuration
# Confidence threshold for label detection (0.0 to 1.0)
# Lower values detect more labels but may include false positives
CONFIDENCE_THRESH=0.04

# Logging Configuration
# Set the logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Print System Configuration
PRINT_ENABLED=true

# Print Command Configuration
# Use 'auto' for system default, or specify custom command with media size
PRINT_COMMAND=auto
# PRINT_COMMAND=lpr -o media=4x6

# Troubleshooting:
# - Set PRINT_DEBUG=true to see detailed print command execution
# - Use /print/status API endpoint to check available printers
# - Use /print/test API endpoint to test printing
# - Run 'lpstat -p' (macOS/Linux) to list available printers

# Optional: Server Configuration
# Uncomment and modify if you need custom server settings
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8000